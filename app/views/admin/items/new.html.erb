<h2>商品の新規登録</h2>

<!-- form_with部分 -->
<%= form_with model: @item, url:'/admin/items', local: true do |f| %>
  <div class="form-group">
    <%= f.label :image_id, '商品画像' %>
    <%= f.attachment_field :image_id %>
  </div>

  <div class="form-group">
  	<%= f.label :name, '商品名' %></br>
    <%= f.text_field :name, class:"form-control" %>
  </div>

  <div class="form-group">
  	<%= f.label :introduction, '商品説明' %></br>
    <%= f.text_area :introduction, class:"form-control" %>
  </div>
  
  <div class="form-group">
    <%= f.label :genre_id,  'ジャンル' %></br>
    <td><%= f.select :genre_id, options_from_collection_for_select(Genre.all, :id, :name), { include_blank: '-- 選択してください --'} %></td>
    </tr>
    <tr>
      <td>税抜価格</td>
      <td><%= f.text_field :price %>円</td>
    </tr>
    <tr>
      <td>販売ステータス</td>
      <td>
        <%= f.radio_button :is_active, "true" %>
        <%= f.label :is_active, "販売中" %>
        <%= f.radio_button :is_active, "false" %>
        <%= f.label :is_active, "販売停止中" %>
      </td>
    </tr>
    <tr>
      <td></td>
      <td>
        <button type="submit" class="btn btn-success">新規登録</button>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>

  </div>

  <div class="form-group">
  	<%= f.label :price, '税抜き価格' %></br>
    <td><%= f.text_field :price, class:"form-control" %></td>円
  </div>
  
  <div class="form-group">
    <tr>
      <td>
        <%= f.radio_button :is_active, "true" %>
        <%= f.label :is_active, "販売中" %>
        <%= f.radio_button :is_active, "false" %>
        <%= f.label :is_active, "販売停止中" %>
      </td>
    </tr>
  </div>

  <div class="form-group">
    <%= f.submit "新規登録", class: "btn btn-success" %>
  </div>
<% end %>